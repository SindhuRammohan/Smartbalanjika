<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.template.SmartBalanjika">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.template.SmartBalanjika.App"
        android:allowBackup="true"
        android:icon="@mipmap/balanjika_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/balanjika_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <activity
            android:theme="@style/AppTheme"
            android:name="com.template.SmartBalanjika.ui.photos.view.MainActivity">

        </activity>

        <activity android:name="com.template.SmartBalanjika.ui.photos.view.WebViewActivity" />

        <activity android:name="com.template.SmartBalanjika.ui.photos.view.ContactDetailsActivity" />
        <activity
            android:name="com.template.SmartBalanjika.ui.splash.view.SplashActivity"
            android:launchMode="singleTask"
            android:theme="@style/SplashScreenTheme"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>

</manifest>